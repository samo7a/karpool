class User {
  User(
      {required this.uid, required this.isVerified, required this.isDriver, required this.isRider});
  String uid;

  bool isVerified;

  bool isDriver;

  bool isRider;
}

// I will add the fields later
